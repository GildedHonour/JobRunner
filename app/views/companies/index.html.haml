.col-sm-9.for.content
  = render "action_panel", search_url: companies_url
  .contacts.holder
    %table.table.for.contacts.table-hover
      %thead
        %tr
          %th
            = check_box_tag "select-contact"
          %th
            Name
          %th
            Affiliation(s)
          %th
            Role(s)
      %tbody#companies-listing
        = render @companies
    #companies-pagination
      = paginate(@companies)
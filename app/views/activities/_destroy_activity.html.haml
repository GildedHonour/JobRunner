%li
  = "<strong>#{activity.whodunnit_email}</strong> deleted the <strong>#{link_to_if(activity.item_page_link.present?, activity.item_description, activity.item_page_link)}</strong> at <em>#{l(activity.created_at)}</em>".html_safe
